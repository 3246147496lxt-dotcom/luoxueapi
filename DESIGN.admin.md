# 落雪 API 管理端 Snow Clay 应用规范

本文件是统一 Luoxue Snow Clay 设计系统在登录后管理场景中的应用规范，不是第二套设计语言。全产品共享 `.superdesign/design-system.md` 的紫色交互层、冰蓝品牌层和 `frontend/src/styles/luoxue-clay-tokens.css`；本文件只补充管理页面的密度、表格、表单和运维规则。历史青绿色方案已经废弃，不得再作为强调色来源。

## 1. 视觉职责

管理端采用三层视觉职责，不能互相替代：

1. **雪花品牌层**：顶部导航、Logo、站点名称和必要的品牌空状态负责“这是落雪 API”。不要把每张卡片都装饰成雪花，也不要用大量冷蓝色模拟冰雪。
2. **紫色交互层**：紫色只表示主操作、当前选择、键盘焦点和可点击强调。非活动导航、普通正文和纯装饰图形不使用高饱和紫色。
3. **语义状态层**：绿色表示成功或健康，琥珀色表示等待或风险，红色表示失败、危险和破坏性操作，蓝色表示信息与实时数据。语义色优先于品牌紫，不能用紫色替代错误或健康状态。

## 2. 唯一基础 Token 来源

共享 CSS 变量位于 `frontend/src/styles/luoxue-clay-tokens.css`，统一使用 `--lx-clay-*` 前缀。首页、管理端外壳、侧栏、Dashboard 与 Teleport 弹层都应引用这里的语义变量，不得在各自文件重新定义同值色板。

核心变量分为：

- `canvas / surface / recessed`：画布、内容表面和内凹表面。
- `text / text-secondary / text-muted`：主次文字层级。
- `accent / info / success / warning / danger`：交互与状态语义。
- `shadow-* / radius-*`：与页面类型对应的层级和形状。
- `font-ui / font-display / font-mono`：界面、少量展示标题与代码字体。

变量同时提供 light、dark 固定色阶和随 `html.dark` 切换的活动别名。普通组件优先使用活动别名；只有自身独立控制主题的表面才能直接使用固定 light/dark 色阶。

Chart.js 与 Canvas 无法直接把未解析的 CSS 自定义属性当作颜色，因此图表色由 `frontend/src/utils/luoxueClayChartTheme.ts` 管理。它是 Canvas 的语义色适配层，不是第二套基础设计 token；画布、文字和主强调仍应与 `--lx-clay-*` 保持同一语义。

## 3. 页面类型契约

页面必须在自己的内容根节点显式声明 `data-admin-page-kind`。禁止根据路由或组件名称隐式推断。

| 类型 | 适用场景 | 表面规则 |
| --- | --- | --- |
| `overview` | 总览、经营 Dashboard | 允许 22–26px 圆角和柔和 Clay 阴影；用于摘要与趋势，不用于长表格套卡片 |
| `table` | 用户、账号、订单、日志等数据管理 | 14px 左右圆角、1px 边界和近乎扁平的阴影；筛选器、表格、分页保持紧凑 |
| `form` | 系统设置、内容配置、方案编辑 | 14–17px 圆角和轻量层级；以分区、标签与保存状态组织内容，避免 Dashboard 式指标卡 |
| `ops` | 运维、风控、实时监控 | 12px 左右圆角、无装饰阴影、高信息密度；健康与风险语义色优先 |

`AdminClayTheme.css` 只消费上述显式属性并提供迁移期的组件样式。新增页面如果不声明类型，应被视为设计契约缺失，而不是自动继承某种页面风格。

## 4. 布局与组件统一

- 保留公开首页与登录页同源的顶部导航；管理端侧栏只负责产品信息架构，不重复站点品牌说明。
- 页面标题统一使用标题、简短说明和操作区；列表页使用紧凑标题，Dashboard 可以更宽松。禁止新增装饰性英文眉题。
- 同一种操作使用同一种组件词汇：保存按钮、筛选控件、Tab、表格、分页、空状态、加载状态和错误状态必须跨页面一致。
- 控件默认高度 40px 左右，触屏关键操作至少 44px；焦点必须清晰，禁用与加载状态不能只靠颜色区分。
- Teleport 的下拉框和对话框使用相同 token，但保持浮层所需的独立阴影；不要让页面类型的扁平规则消除真正的悬浮层级。

## 5. 字体与信息密度

- 日常界面、标签、正文和数据使用 `--lx-clay-font-ui`。
- `--lx-clay-font-display` 只用于 Dashboard 一级标题或少量品牌时刻，不用于按钮、表格表头和表单标签。
- 中文字形会回退到苹方/微软雅黑；英文与数字切换字体时必须确认基线、字重和表格列宽没有跳动。
- 运营列表和监控页允许高密度；密度来自更短的间距和更少的容器层级，不来自缩小正文到难以阅读。

## 6. 使用边界

### 应当

- 用雪花和顶部品牌层建立身份，用紫色建立操作层级。
- 用 1px 边界、间距和背景层次组织大多数管理任务。
- 为成功、警告、错误、加载、空数据和无权限提供明确且一致的反馈。
- 同时验证浅色、深色、侧栏收起、移动端和 Teleport 弹层。

### 不应

- 不要让所有页面都变成大圆角、重阴影的卡片集合。
- 不要嵌套卡片；内容已经位于明确表面时，优先使用分隔线和留白。
- 不要把紫色用于健康、失败、告警等已有标准语义的状态。
- 不要重新引入冰蓝/青色作为管理端主强调色；蓝色只承担信息与数据角色。
- 不要通过全局硬编码选择器继续扩大换肤范围；迁移完成后应逐步用明确组件变体替代兼容性覆盖。

## 7. 验收清单

- 页面根节点声明了正确的 `data-admin-page-kind`。
- 没有新增与 `--lx-clay-*` 重复的核心色值、字体栈或阴影。
- 紫色、蓝色、绿/黄/红的职责没有混用。
- 文字和控件焦点达到清晰可辨的对比度。
- 桌面、移动端、浅色、深色下没有横向溢出或被裁剪的弹层。
- `prefers-reduced-motion: reduce` 下不会依赖动画显示内容。
