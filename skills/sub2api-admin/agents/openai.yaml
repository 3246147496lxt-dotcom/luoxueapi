interface:
  display_name: "LuoxueAPI Admin"
  short_description: "Manage LuoxueAPI admin accounts and settings"
  default_prompt: "Use $sub2api-admin to inspect and manage LuoxueAPI backend accounts safely."

policy:
  allow_implicit_invocation: true
