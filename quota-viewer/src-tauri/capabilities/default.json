{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the independent quota viewer",
  "windows": ["main", "tray"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
